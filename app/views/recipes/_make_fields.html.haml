.nested-fields
  = f.collection_select(:order_id, Order.all, :id, :number, {prompt: ""})
  .f-area
    = f.text_area :sentence, placeholder: "作り方"
    = link_to_remove_association "削除", f, class: "delete-btn"